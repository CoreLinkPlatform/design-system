# Core Color Foundation

## Palette

Core uses semantic colors instead of direct values inside components.

## Primary

- Core Blue 500: `#0066FF`
- Core Cyan 500: `#00C2FF`

## Semantic usage

Components should consume:

- action.primary
- surface.default
- text.primary

Never hardcode product colors in components.
