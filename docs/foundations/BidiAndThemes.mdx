import { Meta } from '@storybook/blocks';

<Meta title="Foundations/RTL, LTR and themes" />

<div className="core-docs">

# RTL, LTR and themes

Direction and theme are independent runtime dimensions. Components must remain usable in Persian RTL and English LTR, in light and dark themes.

<div className="core-docs__bidi">
  <section className="core-docs__demo core-root" lang="fa" dir="rtl"><strong>فارسی · راست‌به‌چپ</strong><p>چیدمان با ویژگی‌های منطقی CSS پیاده‌سازی می‌شود.</p><button className="core-button core-button--primary core-button--md">ادامه</button></section>
  <section className="core-docs__demo core-root" lang="en" dir="ltr"><strong>English · left-to-right</strong><p>One contract supports both writing directions.</p><button className="core-button core-button--primary core-button--md">Continue</button></section>
</div>

## Runtime contract

- Set the content language with `lang="fa"` or `lang="en"`.
- Set direction at the application boundary with `dir="rtl"` or `dir="ltr"`.
- Use logical properties such as `margin-inline-start`; do not mirror with ad-hoc transforms.
- Select themes with `data-theme="light"` or `data-theme="dark"`.
- Use the Storybook toolbar to test both themes; never encode theme colors inside a component.
- Focus remains a 2px Core Blue ring with a 2px offset in every mode.

</div>
