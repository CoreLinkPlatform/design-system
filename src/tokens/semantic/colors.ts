import { primitiveColors } from '../primitives/colors';

export const semanticColors = {
  actionPrimary: primitiveColors.blue[500],
  actionPrimaryHover: primitiveColors.blue[600],
  surfaceDefault: '#FFFFFF',
  textPrimary: primitiveColors.gray[900],
} as const;
