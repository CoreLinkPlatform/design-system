{
  "name": "@coreplatform/design-system",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
